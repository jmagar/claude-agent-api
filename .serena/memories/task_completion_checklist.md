# Task Completion Checklist

- Run `make lint` (ruff check)
- Run `make typecheck` (ty)
- Run `make test` or `make test-fast` depending on scope
- Ensure no `Any` types or `# type: ignore`

Source: `CLAUDE.md`, `pyproject.toml`.